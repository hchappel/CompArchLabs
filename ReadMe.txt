Take in four arguments and simulates a cache with no Locality.

To run do as follows:

Javac *.java

Java CacheProject (int Cache Size) (int Block Size) (boolean FIFO(true)/LRU(false)) (boolean Associativity(true fully/ false direct))

Output_1: array of final cache results 
Output_2: number of hits
